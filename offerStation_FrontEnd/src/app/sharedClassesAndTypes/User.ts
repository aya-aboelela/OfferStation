import { AddressDetails } from "./AddressDetails";

export class User {
    constructor(
        FirstName: string,
        LastName: string,
        Phone: string,
        Address: AddressDetails,
        Password: string,
        Email: string,
    ) { }
}